package com.yourname.womensafety.ui.theme

import androidx.compose.ui.graphics.Color

// ── Primary Brand (Neon Rose Theme) ───────────────────────────────────────────
val RedPrimary    = Color(0xFFE25F71)   // Neon Rose — Main accent
val RedGlow       = Color(0xFFE65C78)   // Coral Rose — Glow effects
val RedDeep       = Color(0xFF280000)   // Deep red/black for gradients end
val RedSoft       = Color(0xFFE15E71)   // Rose Pink — Softer variant

// ── Background Gradient (Sleek Dark Theme) ───────────────────────────────────
val BgDark        = Color(0xFF000000)   // Pure black
val BgMid         = Color(0xFF080404)   // Very dark warm black
val BgEnd         = Color(0xFF120508)   // Deepest red-black end

// ── Pink Accents ──────────────────────────────────────────────────────────────
val PinkBase      = Color(0xFFE15E71)   // Rose pink for highlights
val PinkAccent    = Color(0xFFE04B6A)   // Raspberry rose for cards/borders
val PinkSoft      = Color(0xFFF09CA8)   // Very soft pink for text on dark

// ── Surface / Card ───────────────────────────────────────────────────────────
val CardBg        = Color(0xFF160E0E)   // Warm dark gray-red card (from original image aesthetic)
val CardBgAlt     = Color(0xFF1C1212)   // Slightly warmer card variant
val CardBgDark    = Color(0xFF0F0B0B)   // Darkest card variant

// ── Navigation ────────────────────────────────────────────────────────────────
val NavBarBg      = Color(0xFF0A0505)   // Bottom nav background

// ── Status Colors ─────────────────────────────────────────────────────────────
val GreenStatus   = Color(0xFF4CAF50)   // Success / verified / safe
val GreenGlow     = Color(0xFF00E676)   // Brighter green for emphasis
val AmberWarning  = Color(0xFFFFB74D)   // Warning / cancelled
val BlueInfo      = Color(0xFF4FC3F7)   // Auto-SOS / info
val PurpleAlt     = Color(0xFF9C8FFF)   // Security / privacy

// ── Text ──────────────────────────────────────────────────────────────────────
val White         = Color(0xFFFFFFFF)
val TextGray      = Color(0xFFA0A0A0)
val TextMuted     = Color(0xFF666666)

// ── Dividers / Borders ───────────────────────────────────────────────────────
val DividerColor  = Color(0xFF281016)
val BorderSubtle  = Color(0xFF1F0D11)

// Legacy alias kept for backward compat
val ProtectionOffBg = Color(0xFF252B35)
val GradientDark    = BgDark
val GradientMid     = BgMid
val GradientEnd     = BgEnd
